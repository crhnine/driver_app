<?php

        if($current_login !== ""){
            if($url === "lower_directory"){

            echo '<a href="../submissions/logout_function.php"><button class="show_logout">Logout</button></a>';

            }

        }

?>